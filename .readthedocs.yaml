# .readthedocs.yaml

version: 2
formats:
  - pdf
  - epub

build:
  os: ubuntu-22.04
  tools:
    python: "3.11.1"

python:
  install:
    - method: pip
      path: .
    - requirements: docs/requirements.txt
